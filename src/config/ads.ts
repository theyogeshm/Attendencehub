/**
 * Central advertisement configuration
 *
 * ADS_ENABLED controls whether ad container placeholders render at all.
 * When false (default), every <AdSlot /> component returns null with ZERO layout impact,
 * zero DOM elements, and zero margin/padding.
 */
export const ADS_ENABLED = false;

// Default AdSense client ID (to be replaced with live pub ID when enabled)
export const ADSENSE_CLIENT_ID = "ca-pub-XXXXXXXXXXXXXXXX";
